/// The current version of the `flutter_viewmodel` package.
///
/// This is the single source of truth for the package version in code.
/// It must be kept in sync with the `version` field in `pubspec.yaml`
/// and the latest released version in `CHANGELOG.md`.
///
/// A test (`test/version_test.dart`) verifies that this value matches the
/// version declared in `pubspec.yaml`.
const String packageVersion = '0.0.1';
